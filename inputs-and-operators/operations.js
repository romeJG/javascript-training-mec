// js operators

{
    var gX = "hello"
    let x = "string 1 "
    y = "string 2"
    z = x + y // will output "string 1 string 2"
    console.log(z)
}
{
    //multiply two numbers then output the result
    let x = 2
    y = 3
    z = x * y
    console.log("output of x * y is " + z)
}
{
    //divide two numbers then output the result
    let x = 10
    y = 4
    z = x / y
    console.log("output of x / y is " + z)
}
{
    //modulus two numbers then output the result
    let x = 10
    y = 4
    z = x % y
    console.log("output of x % y is " + z)
    console.log(gX)
}
